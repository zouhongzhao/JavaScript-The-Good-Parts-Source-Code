chapter: Style
==================
if (a)
    b(  );
    
    
====================================
if (a)
    b(  );
    c(  );
    
    
====================================
if (a) {
    b(  );
    c(  );
}
    
    
====================================
if (a) {
    b(  );
}
c(  );
    
    
====================================
i = 0; // Set i to zero.
    
    
====================================
if (a = b) { ... }
    
    
====================================
if (a === b) { ... }
    
    
==================